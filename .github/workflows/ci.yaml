name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  canonical:
    name: Canonical gates
    uses: ./.github/workflows/canonical-gates.yaml
    permissions:
      contents: read
